FROM node:20

# Set the environment variable
ENV NODE_OPTIONS=--openssl-legacy-provider

WORKDIR /usr/src/app

COPY package*.json ./

RUN npm install -g pnpm
RUN pnpm install

COPY . .

RUN pnpm run build

CMD ["pnpm", "run", "serve"]
