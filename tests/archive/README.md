# Archived tests

Coverage-only tests (written to hit coverage targets, not to assert product behavior). Excluded from default pytest run. See root pytest.ini / pyproject.toml norecursedirs.

Safe to delete this folder if you no longer need these tests.
