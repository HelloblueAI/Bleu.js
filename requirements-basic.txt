# Pinned constraints for server + ML installs (legacy constraint file).
# Prefer: pip install -e ".[server,ml]" or pip install "bleu-js[ml]"
# See requirements.txt / pyproject.toml extras for canonical installs.

aiohttp>=3.9.3
alembic==1.18.4
aws-lambda-powertools>=3.30.0

# AWS
boto3>=1.24.0
cryptography>=42.0.5

# Web framework
fastapi==0.136.3
httpx>=0.27.0
# Core dependencies
numpy>=1.24.3,<3.0.0
pandas>=2.0.0
psycopg2-binary==2.9.12
pydantic[email]==2.13.4

# Utilities
python-dotenv>=0.20.0
requests>=2.32.4
scikit-learn>=1.2.2

# Database
sqlalchemy==2.0.50

# Security
urllib3>=2.6.3  # Fixed CVE-2026-21441 (decompression-bomb safeguard bypass)
uvicorn==0.49.0
