{
  "name": "language-plugins",
  "version": "1.0.18",
  "private": true,
  "workspaces": [
    "javascript",
    "python"
  ],
  "dependencies": {
    "force": "^0.0.3"
  },
  "scripts": {
    "start": "pnpm run start --workspace=javascript",
    "build": "pnpm run build --workspace=python",
    "test": "pnpm run test"
  },
  "devDependencies": {
    "eslint": "^8.40.0",
    "prettier": "^2.8.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
